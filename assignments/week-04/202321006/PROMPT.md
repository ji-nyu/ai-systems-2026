# Ralph 루프 실습 태스크: 계산기(calc.py) 구현

## 목표
- Python으로 더하기(`add`), 빼기(`subtract`), 나누기(`divide`) 기능을 포함한 `calc.py`를 작성하세요.
- 나누기 기능 시 0으로 나누는 경우에 대한 예외 처리를 포함하세요.
- 모든 기능은 `test_calc.py`에 작성된 테스트를 통과해야 합니다.

## 제약 사항
- 외부 라이브러리 사용 금지 (순수 Python만 사용).
- `calc.py`와 `test_calc.py`는 동일한 디렉토리에 있어야 함.
- 타입 힌트(`typing`)를 사용하여 견고한 코드를 작성하세요.
